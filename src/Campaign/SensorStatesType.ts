/**
 * Represents for each sensors if it has been enabled or not for this campaign.
 */
export type SensorStates = {
    o2: boolean;
    co2: boolean;
    humidity: boolean;
    luminosity: boolean;
    temperature: boolean;
}